# ~/.ssh/config
# SSH 設定ファイル

# ==================== 一般設定 ====================

Host *
    # エージェントへの鍵追加
    AddKeysToAgent yes
    
    # 特定の鍵のみ使用
    IdentitiesOnly yes
    
    # 接続維持
    ServerAliveInterval 60
    ServerAliveCountMax 3
    
    # 圧縮
    Compression yes
    
{{ if .is_mac -}}
    # macOS キーチェイン
    UseKeychain yes
{{ end -}}

# config.d ディレクトリからインクルード
Include ~/.ssh/config.d/*
